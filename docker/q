FROM ubuntu:latest

RUN add-apt-repository ppa:neovim-ppa/stable \
    apt-get update && apt-get install -y \
    neovim \
    git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /root/.config/nvim

RUN git clone https://github.com/itsvinayak/NeoMinimalVim.nvim.git .

CMD ["nvim"]


